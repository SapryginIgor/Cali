# Debug Skill
1. First, read the error message carefully
2. Run `git status` to check branch state
3. Trace the error to its source file using Grep
4. Propose a minimal fix and validate syntax before applying
5. Run the failing command again to confirm the fix
